module.exports = (client, message) => {

    if(message.content.startsWith("Прив")) {
        message.react("👋")
    }

    if(message.content.startsWith("Хай")) {
        message.react("👋")
    }

    if(message.content.startsWith("Здар")) {
        message.react("👋")
    }

    if(message.content.startsWith("Здор")) {
        message.react("👋")
    }

    if(message.content.startsWith("Драс")) {
        message.react("👋")
    }

    if(message.content.startsWith("Здрас")) {
        message.react("👋")
    }
}